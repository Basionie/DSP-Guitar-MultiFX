#include "config/presets.h"

const int preset_values[3][3] = {
    {42, 61, 55}, // Delay (Time, Feedback, Wet)
    {90, 30, 60}, // Reverb (Size, Damping, Wet)
    {4,  66, 50}  // Chorus (Rate, Depth, Base)
};